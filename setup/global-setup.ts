

async function globalSetup() {
  console.log('GLOBAL SETUP: Starting global setup - runs once before all tests');
  // Example: You could start a test database, authentication service, etc.
}

export default globalSetup;
